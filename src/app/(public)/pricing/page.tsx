import PricingComponent from "@/components/pages/pricing";

export const metadata = {
  title: "Pricing - Imagify",
  description:
    "Simple, pay-as-you-go pricing for AI image processing tools. Purchase prepaid credits and use them across any tool.",
};

export default function PricingPage() {
  return <PricingComponent />;
}
