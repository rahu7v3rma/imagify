import CropRotateFlipComponent from '@/components/pages/crop-rotate-flip';

export const metadata = {
  title: 'Crop-Rotate-Flip Image - Imagify',
  description:
    'Crop, rotate, and flip your images with ease. Upload your image and transform it instantly.',
};

export default function CropRotateFlipPage() {
  return <CropRotateFlipComponent />;
}
