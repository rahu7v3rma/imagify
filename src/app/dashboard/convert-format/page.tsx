import ConvertFormatComponent from "@/components/pages/convert-format";

export const metadata = {
  title: "Convert Image Format - Imagify",
  description:
    "Convert images between different formats like PNG, JPEG, JPG, and WebP. Upload your image and choose the output format.",
};

export default function ConvertFormatPage() {
  return <ConvertFormatComponent />;
}
