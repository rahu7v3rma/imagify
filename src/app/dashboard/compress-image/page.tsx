import CompressImageComponent from "@/components/pages/compress-image";

export const metadata = {
  title: "Compress Image - Imagify",
  description:
    "Reduce image file size without losing quality using AI. Upload your image and compress it instantly.",
};

export default function CompressImagePage() {
  return <CompressImageComponent />;
}
